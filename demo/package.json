{
  "name": "deborah-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deborah — demo (Node.js/Express). index + cast focus.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20.12.0"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "playwright": "^1.62.1"
  }
}
