/**
 * Deborah — Institutional Handoff Barrel Export
 *
 * Prompt 72 — final migration (cutover), role training, controlled pilot va
 * procurement pack (HECVAT/ACR/security/DPA/SLA/exit). Har write path
 * tenant-scoped + authorized + validated + idempotent; har privileged action
 * audited (item 16–17).
 *
 * Usage:
 *   import * as institutional from '../modules/institutional/index.js';
 */

export * from './institutional.schema.js';
export * from './institutional.service.js';
