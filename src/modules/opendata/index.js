/**
 * Deborah — Ochiq ma'lumotlar moduli (AUTH A-13)
 */
export {
  getStats,
  refreshDataset,
  normalizeDataset,
  fetchDatasetUrl,
  isAllowedHost,
  isEnabled,
  OpenDataError,
  ALLOWED_HOSTS,
  SCHEMA_VERSION,
} from './universities.js';
