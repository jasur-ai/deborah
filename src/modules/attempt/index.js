/**
 * Deborah — Attempt Lease, Identity Step & Server Timer (Prompt 30)
 *
 * Phase D #1: secure, single-writer, server-timed attempt start.
 *
 * Usage:
 *   import * as attempt from '../modules/attempt/index.js';
 *   // Or:
 *   import { startAttempt } from '../modules/attempt/index.js';
 */

export * from './attempt.schema.js';
export * from './attempt.service.js';
